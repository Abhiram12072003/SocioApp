{
    "version": 2,
    "builds": [
        {
            "src": "*.js",
            "use": "@vercel/node"
        }
    ],
    "installCommand": "npm install --force",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}
